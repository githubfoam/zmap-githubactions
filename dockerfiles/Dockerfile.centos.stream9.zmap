# https://quay.io/repository/centos/centos?tab=tags&tag=stream9
# FROM centos:centos9
# FROM quay.io/centos/centos9:latest
FROM quay.io/centos/centos:stream9

LABEL org.opencontainers.image.authors="githubfoam"

RUN set -xe && \
    yum update -qy && \
    yum install zmap

ENTRYPOINT ["zmap"]
CMD ["-h"]

